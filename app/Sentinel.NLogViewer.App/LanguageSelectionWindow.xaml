<Window x:Class="Sentinel.NLogViewer.App.LanguageSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Select Language" 
        Height="400" 
        Width="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Text="Select Language" 
                   FontSize="18" 
                   FontWeight="Bold"
                   Margin="0,0,0,15"
                   Grid.Row="0"/>

        <!-- Language List -->
        <ListBox x:Name="LanguageListBox"
                 Grid.Row="1"
                 ItemsSource="{Binding Languages}"
                 SelectedItem="{Binding SelectedLanguage}"
                 Margin="0,0,0,15">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <Image Source="{Binding FlagPath, Converter={StaticResource SvgImageSourceConverter}}" 
                               Width="30" 
                               Height="20" 
                               Stretch="Uniform"
                               Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding Name}" 
                                   FontSize="14"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="OK" 
                    IsDefault="True"
                    Click="OkButton_Click"
                    Margin="0,0,5,0"
                    Padding="20,5"
                    MinWidth="80"/>
            <Button Content="Cancel" 
                    IsCancel="True"
                    Padding="20,5"
                    MinWidth="80"/>
        </StackPanel>
    </Grid>
</Window>

